{
    "extends": [
        "config:base"
    ],
    "assignees": ["simpod"],
    "packageRules": [
        {
            "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
            "automerge": true
        }
    ],
    "rangeStrategy": "replace"
}
